<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Welcome to Coatcard AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Gradient Background for the Body */
            background: linear-gradient(135deg, #A8B8FF 0%, #88D3CE 100%); /* A pleasant blue-green gradient */
            background-size: 400% 400%; /* Larger background for animation */
            animation: gradientAnimation 15s ease infinite; /* Smooth, continuous gradient animation */
        }

        /* Keyframe animation for the background gradient */
        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Glassmorphism Effect for the main content box */
        .glass-effect {
            background: rgba(255, 255, 255, 0.15); /* Slightly transparent white background */
            backdrop-filter: blur(10px); /* The key to the glass effect */
            -webkit-backdrop-filter: blur(10px); /* For Safari support */
            border: 1px solid rgba(255, 255, 255, 0.2); /* Subtle light border */
            border-radius: 16px; /* Rounded corners for the glass card */
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1); /* Soft shadow */
            animation: fadeInScale 1s ease-out forwards; /* Fade in and slightly scale up on load */
        }

        /* Animation for the main content box */
        @keyframes fadeInScale {
            from {
                opacity: 0;
                transform: scale(0.95) translateY(20px);
            }
            to {
                opacity: 1;
                transform: scale(1) translateY(0);
            }
        }

        /* Pulsing animation for the main title */
        .pulse-title {
            animation: pulseEffect 2s infinite ease-in-out;
        }

        @keyframes pulseEffect {
            0% { transform: scale(1); }
            50% { transform: scale(1.03); }
            100% { transform: scale(1); }
        }

        /* Button specific styles for a modern look */
        .btn-gradient-1 {
            background: linear-gradient(45deg, #6A82FB, #FC5C7D); /* Blue to Pink gradient */
            color: white; /* White text for contrast */
            transition: all 0.3s ease; /* Smooth transition for hover effects */
            position: relative;
            overflow: hidden; /* Hide overflow for shine effect */
        }

        .btn-gradient-1:hover {
            transform: scale(1.05); /* Slightly larger on hover */
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2); /* More prominent shadow on hover */
        }

        .btn-gradient-2 {
            background: linear-gradient(45deg, #a1c4fd, #c2e9fb); /* Light blue to very light blue gradient */
            color: #333; /* Darker text for contrast */
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-gradient-2:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
        }

        /* Shine effect on hover for buttons */
        .btn-gradient-1::after,
        .btn-gradient-2::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.3); /* White translucent shine */
            transform: skewX(-30deg);
            transition: 0.5s;
        }

        .btn-gradient-1:hover::after,
        .btn-gradient-2:hover::after {
            left: 100%; /* Move shine across */
        }
    </style>
</head>
<body class="flex items-center justify-center h-screen">
    <div class="text-center p-8 glass-effect"> <div class="mb-8">
            <h1 class="text-6xl font-bold text-white pulse-title">Coatcard AI</h1> <p class="text-xl text-gray-200 mt-2">Your Personal Coding Assistant</p> </div>
        <div class="space-x-4">
            <a href="/login" class="px-8 py-3 font-semibold rounded-lg shadow-md btn-gradient-1">
                Login
            </a>
            <a href="/register" class="px-8 py-3 font-semibold rounded-lg shadow-md btn-gradient-2">
                Register
            </a>
        </div>
    </div>
</body>
</html>